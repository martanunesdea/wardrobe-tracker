# Required in production (set via `fly secrets set`):
JWT_SECRET=change-me-to-a-long-random-string

# Defaults baked into fly.toml / Dockerfile (override if needed):
# PORT=8000
 FLASK_DEBUG=true
# DB_PATH=/data/wardrobe.db
# UPLOAD_FOLDER=/data/uploads
# CORS_ORIGINS=https://wardrobe-app.fly.dev
# MAX_CONTENT_LENGTH=5242880
